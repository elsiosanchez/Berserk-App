import 'package:flutter/material.dart';

class BerserkColors {
  BerserkColors._();

  // Base
  static const bg = Color(0xFF1C1C1E);
  static const card = Color(0xFF2C2C2E);
  static const card2 = Color(0xFF3A3A3C);
  static const separator = Color(0x0FFFFFFF);

  // Text
  static const textPrimary = Color(0xFFFFFFFF);
  static const textSecondary = Color(0xFF8E8E93);
  static const textTertiary = Color(0xFF636366);

  // Accent
  static const accent = Color(0xFFC0392B);
  static const amber = Color(0xFFE8A838);
  static const success = Color(0xFF27AE60);
  static const info = Color(0xFF3A8EFF);

  // Muscle groups
  static const chest = Color(0xFFC0392B);
  static const back = Color(0xFF3A8EFF);
  static const legs = Color(0xFFA855F7);
  static const shoulders = Color(0xFFE8A838);
  static const arms = Color(0xFFD4537E);

  // Checkpoint states
  static const done = Color(0xFF27AE60);
  static const today = Color(0xFFC0392B);
  static const pending = Color(0xFF3A3A3C);
  static const missed = Color(0xFFC0392B);
}
